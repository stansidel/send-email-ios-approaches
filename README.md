# Ways of Sending Emails from an iOS App

There're three ways of sending an email from an iOS app:

1. Using `MFMailComposeViewController`.
2. Through openning a `URL`.
3. Using `UIActivityViewController`.
